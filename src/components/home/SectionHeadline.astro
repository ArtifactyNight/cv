---
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<h2
  id={id}
  class="scroll-mt-8 mb-4 font-sentient text-base font-medium italic leading-snug text-ink-muted"
>
  <slot />
</h2>
